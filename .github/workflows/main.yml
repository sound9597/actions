name: Deploy to EKS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'

    - name: Install dependencies
      run: npm install

    - name: Build React app
      run: npm run build

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ AKIA3FLD43WGVQ7DQNAV }}
        aws-secret-access-key: ${{ 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 }}
        aws-region: 'us-east-1'

    - name: Login to Amazon EKS
      run: aws eks --region <us-east-1> update-kubeconfig --name <react-js>

    - name: Deploy to EKS
      run: kubectl apply -f kube/deployment.yaml
